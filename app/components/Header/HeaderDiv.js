import styled from 'styled-components';

import HorizontalFlex from 'components/HorizontalFlex';

const HeaderDiv = styled(HorizontalFlex)`
	box-shadow: 0 0 2px rgba(0,0,0,.5);
	margin-bottom: 2px;
`;

export default HeaderDiv;