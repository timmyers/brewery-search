import styled from 'styled-components';

import HorizontalFlex from 'components/HorizontalFlex';

const FooterDiv = styled(HorizontalFlex)`
	box-shadow: 0 0 2px rgba(0,0,0,.5);
	margin-top: 2px;
`;

export default FooterDiv;